#!/usr/bin/env bash
# Test stub: minimal output for prereq connectivity checks. Delegates other kubectl to /usr/bin/kubectl.
set -euo pipefail
if [[ "${1-}" == "cluster-info" ]]; then
  echo "Kubernetes control plane is running at https://stub.example"
  exit 0
fi
if [[ "${1-}" == "get" && "${2-}" == "nodes" ]]; then
  echo "NAME   STATUS   ROLES   AGE   VERSION"
  echo "stub   Ready    <none>   1d   v0.0.0"
  exit 0
fi
exec /usr/bin/kubectl "$@"
